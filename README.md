# VINCE Ops

App de operação de projetos (React + TypeScript + Vite + Supabase).

## Docs

Orientação, planos e decisões de arquitetura: **[docs/](./docs/README.md)**.

## Dev

```bash
npm install
npm run dev
npm run build
```

Variáveis locais em `.env.local` (não versionado).
